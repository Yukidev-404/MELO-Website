(()=>{const render=async()=>{const p=new URLSearchParams(location.search);if((p.get('view')||'dashboard')!=='flags')return;const c=document.getElementById('page-content');if(!c||typeof api!=='function')return;try{const d=await api('/api/admin/flags'),rows=d.rows||[],enabled=rows.filter(r=>Number(r.enabled)).length;c.innerHTML=`<section class="page flags-page"><div class="page-intro"><div><h2>Feature Flags</h2><p>Controlled product switches stored in D1. Owner-only changes are explicit, confirmed and server-audited.</p></div><span class="flag-control-note">OWNER CONTROL</span></div><section class="metrics">${metric('TOTAL FLAGS',rows.length,'Configured switches')}${metric('ENABLED',enabled,'Currently ON')}${metric('DISABLED',rows.length-enabled,'Currently OFF')}${metric('CONTROL MODE','AUDITED','Server-side mutations')}</section><section class="panel"><div class="panel-head"><h2>Flag registry</h2><span>LIVE D1 DATA</span></div><div class="flag-list">${rows.length?rows.map(r=>`<article class="flag-card"><div class="flag-main"><div><code>${esc(r.flag_key)}</code><p>${esc(r.description||'No description')}</p></div><div><div class="flag-state ${Number(r.enabled)?'on':'off'}"><i></i>${Number(r.enabled)?'ENABLED':'DISABLED'}</div><button class="flag-toggle" data-key="${esc(r.flag_key)}" data-enabled="${Number(r.enabled)?'1':'0'}">${Number(r.enabled)?'DISABLE':'ENABLE'}</button></div></div><div class="flag-meta"><span>UPDATED ${esc(date(r.updated_at))}</span><span>CREATED ${esc(date(r.created_at))}</span><span class="flag-audit">AUDITED</span></div></article>`).join(''):'<div class="empty">No feature flags configured.</div>'}</div></section><div class="health-strip"><span><strong>SERVER-SIDE CONTROL.</strong> Flag mutations require Owner authorization and are written to Audit Log.</span><span class="right">${rows.length} FLAG${rows.length===1?'':'S'}</span></div></section>`;c.querySelectorAll('.flag-toggle').forEach(b=>b.onclick=async()=>{const key=b.dataset.key,next=b.dataset.enabled!=='1';if(!confirm(`${next?'Enable':'Disable'} feature flag “${key}”? This change is recorded in Audit Log.`))return;b.disabled=true;try{const r=await fetch('/api/admin/flag',{method:'POST',credentials:'include',cache:'no-store',headers:{'Content-Type':'application/json'},body:JSON.stringify({flag_key:key,enabled:next})});const x=await r.json().catch(()=>({}));if(r.status===401){location.href='../admin-login.html';return}if(!r.ok)throw Error(x.error||`Request failed (${r.status})`);await render()}catch(e){alert(e.message);b.disabled=false}})}catch(e){c.innerHTML=`<section class="page"><div class="empty">Unable to load feature flags. ${esc(e.message)}</div></section>`}};window.addEventListener('popstate',render);const oldPush=history.pushState;history.pushState=function(){const r=oldPush.apply(this,arguments);setTimeout(render,0);return r};const oldReplace=history.replaceState;history.replaceState=function(){const r=oldReplace.apply(this,arguments);setTimeout(render,0);return r};setTimeout(render,0)})();