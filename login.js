const modes=document.querySelectorAll('.mode');const form=document.getElementById('loginForm');const message=document.getElementById('demoMessage');let mode='user';modes.forEach(btn=>btn.addEventListener('click',()=>{modes.forEach(b=>b.classList.remove('active'));btn.classList.add('active');mode=btn.dataset.mode;message.textContent='';document.querySelector('.card-top span').textContent=mode==='admin'?'[ ADMIN CONSOLE ]':'[ MELO ACCOUNT ]'}));form.addEventListener('submit',e=>{e.preventDefault();message.textContent=mode==='admin'?'Admin authentication will be connected later.':'Account authentication will be connected later.'});document.querySelectorAll('.social-login').forEach(button=>button.addEventListener('click',()=>{message.textContent=`${button.dataset.provider} authentication will be connected later.`}));
